import {StyleSheet} from 'react-native';
import {theme} from '../theme';

const color = StyleSheet.create({
  primary_light: {
    color: theme.dark,
  },
  primary: {
    color: theme.dark,
  },
  primary_dark: {
    color: theme.light,
  },
  secondary_light: {
    color: theme.dark,
  },
  secondary: {
    color: theme.dark,
  },
  secondary_dark: {
    color: theme.light,
  },
  light: {
    color: theme.dark,
  },
  dark: {
    color: theme.light,
  },
});

export {color};
