import {StyleSheet} from 'react-native';

const border = StyleSheet.create({
  r50: {
    borderRadius: 50,
    overflow: 'hidden',
  },
  r10: {
    borderRadius: 10,
    overflow: 'hidden',
  },
});

export {border};
